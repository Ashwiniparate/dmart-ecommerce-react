

function E({data4}){//its called props drilling but it is very confusion its used only if we have 1 level or 2 level only
    return(
        <div>
            <h1>E component :{data4}</h1>
             
        </div>
    );
}

export default E;